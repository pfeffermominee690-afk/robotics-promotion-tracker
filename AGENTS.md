# AGENTS.md

本文件适用于本仓库及其所有子目录。任何维护者或 Codex 在新增、修改通知数据前，都必须遵守以下规则。

## 项目边界

本项目只收录同时满足以下条件的通知：

1. 由 985 高校、研究生院、招生办公室或所属学院的官方域名发布，并且链接直达具体通知正文。
2. 官方页面的发布时间为 2026 年。
3. 内容属于 2026 年夏令营，或面向 2027 级研究生招生的预推免通知。
4. 通知正文中的专业、学科或研究方向使机器人工程本科生能够合理申请，包括机器人、控制、自动化、机械、人工智能、计算机、电子信息、仪器、无人系统等相关方向。

不得收录：

- 学院首页、学校首页或研招网首页，而非具体通知页面。
- 培训机构、考研信息站、聚合站、自媒体或其他第三方转载。
- 标题年份、招生年份与官方发布时间明显不一致的旧通知。
- 只在搜索摘要中出现、正文无法打开或来源无法核实的信息。
- 无法从正文确认，却依靠常识、往年安排或猜测补出的截止时间和申请条件。

## 新增或更新记录前的强制核验

1. 打开 `officialUrl` 指向的官方通知正文，不得仅查看搜索结果摘要。
2. 核对发布单位、完整标题、发布日期、招生年份、通知类别和报名截止日期。
3. 阅读专业范围与申请条件，确认机器人工程本科生能够合理申请；在 `roboticsEligibility` 中简洁写明正文依据或审慎判断。
4. 保留官方原始链接，不得替换成学院首页、研招网首页、缓存页或转载页。
5. 用官方链接、标题和学校/学院组合检查重复记录。相同通知不得因多个方向而拆成多条。
6. 所有字段都必须存在。未知但允许为空的内容使用空字符串，不得删除字段。
7. 将 `lastVerified` 更新为本次实际打开正文并核验的日期。
8. 根据当前日期重新计算 `status`，完成后运行检查脚本。

## 字段规范

- `id`：稳定、唯一，建议使用小写英文、数字和连字符；记录更新时不要随意更换。
- `university`、`college`、`title`：以官方正文为准，不擅自缩写。
- `category`：只能是 `夏令营` 或 `预推免`。
- `admissionYear`：本项目当前周期固定为数字 `2027`。
- `directions`：字符串数组，只写正文支持且与本项目范围相关的方向。
- `roboticsEligibility`：说明机器人工程背景为何可以合理申请。正文仅列专业大类时应如实说明，不得写成“明确接受”。
- `matchLevel`：只能是 `高`、`中`、`低`。机器人、控制、自动化等直接方向通常为高；有明确交叉入口的机械、人工智能、计算机、电子信息、仪器、无人系统可按正文判断为中或高；仅有宽泛工科入口且适配较弱时为低。
- `publishDate`、`deadline`、`lastVerified`：统一使用 `YYYY-MM-DD`。截止日期未公布时 `deadline` 使用空字符串。
- `notes`：记录特殊限制、时间说明或链接核验情况；没有补充时使用空字符串。

## 状态计算规则

状态以维护当天的本地日期为准：

- `报名中`：截止日期在 7 天以后。
- `即将截止`：截止日期为今天或未来 7 天内。
- `已截止`：截止日期早于今天。
- `待发布`：官方通知存在，但报名时间或截止时间尚未发布；此时 `deadline` 应为空。
- `链接待核验`：原始链接失效、出现 404/410，或无法确认页面是否仍为原通知。这个状态优先于日期状态。

网页会按访问当天再次计算前三种日期状态，防止静态数据在两次维护之间显示过期；数据文件中的 `status` 仍必须在每次修改时同步更新。

## 链接失效处理

- 链接失效时不得直接删除记录，也不得擅自换成首页或第三方转载。
- 保留原 `officialUrl`，把 `status` 标记为 `链接待核验`。
- 在 `notes` 中写明发现问题的日期和现象，例如“2026-08-06 核验时返回 404，链接待核验”。
- 只有找到同一官方单位发布的迁移后正文，且标题与内容一致时，才可更新链接；更新后同时更新 `lastVerified` 和状态。

## 推荐更新流程

1. 阅读本文件和现有 `data/notices.json`。
2. 逐条打开用户提供的官方页面并记录核验结果。
3. 对符合标准的通知新增或更新记录，对不符合标准的通知说明跳过原因。
4. 检查重复项，并按当前日期更新所有受影响记录的 `status` 和 `lastVerified`。
5. 在项目根目录运行：

   ```bash
   python scripts/check_links.py
   ```

6. 修复所有数据错误。遇到 403、验证码或网络超时等无法自动判断的情况，应人工打开页面核验，不能把“脚本无法访问”直接等同于“链接失效”。
7. 启动本地静态服务器，确认页面能加载，并抽查新增通知的搜索、筛选、排序和官方链接。

## 代码维护要求

- 保持项目为无需构建步骤的纯静态网站，不引入服务器端代码、数据库或付费服务。
- 资源路径必须使用相对路径，确保能从 GitHub Pages 的仓库子路径发布。
- 修改界面时保留键盘可访问性、手机布局、外链新标签页和中文界面。
- 不得为了让检查脚本通过而弱化来源、年份、日期或必填字段校验。
- 除非用户明确要求，不得在 `notices.json` 中加入演示、占位或未经核验的记录。
